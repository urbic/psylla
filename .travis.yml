language: java

dist: bionic

sudo: required

group: edge

jdk:
    - oraclejdk8
    - openjdk11

#install: ant -v test

addons:
  apt:
    packages:
      - javacc
      - junit4
      - libjline-java
      - ant-optional

