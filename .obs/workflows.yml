workflow:
  steps:
    - rebuild_package:
      project: home:urbic:java
      source_package: psylla
  filters:
    event: push
    branches:
      only:
        - master
      ignore:
        - gh-pages
