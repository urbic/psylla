package coneforest.psylla.test;

public class PsyllaTest
{

	/*
	public static void run(final String[] args)
	{
		coneforest.psylla.Psylla.main(args);
	}

	public static void runScript(final String scriptName, final String[] scriptArgs, final String[] psyllaArgs)
	{
		final String[] cmdLine=new String[psyllaArgs.length+1+scriptArgs.length];
		for(int i=0; i<psyllaArgs.length; i++)
			cmdLine[i]=psyllaArgs[i];
		cmdLine[psyllaArgs.length]=scriptName;
		for(int i=0; i<scriptArgs.length; i++)
			cmdLine[psyllaArgs.length+1+i]=scriptArgs[i];
		run(cmdLine);
	}

	public static void runExpr(final String expr, final String[] exprArgs, final String[] psyllaArgs)
	{
		final String[] cmdLine=new String[psyllaArgs.length+2];
		for(int i=0; i<psyllaArgs.length; i++)
			cmdLine[i]=psyllaArgs[i];
		cmdLine[psyllaArgs.length]="-e";
		cmdLine[psyllaArgs.length+1]=expr;
		for(int i=0; i<exprArgs.length; i++)
			cmdLine[psyllaArgs.length+2+i]=exprArgs[i];
		run(cmdLine);
	}

	public static void runExpr(final String expr, final String[] exprArgs)
	{
		runExpr(expr, exprArgs, new String[0]);
	}

	public static void runExpr(final String expr)
	{
		runExpr(expr, new String[0]);
	}
	*/
}
