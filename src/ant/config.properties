project.version.major=0
project.version.minor=10
project.version.revision=0
project.version.type=
project.version=${project.version.major}.${project.version.minor}.${project.version.revision}
project.name=psylla
project.description=\
Psylla is scriptable interpretive concatenative homoiconic PostScript-like programming language.${line.separator}\
This package contains extensible and embeddable Psylla implementation written in Java.
project.summary=The Psylla programming language
config.sysconfdir=/etc
config.prefix=/usr
config.datadir=${config.prefix}/share
config.libdir=${config.prefix}/lib
config.lib64dir=${config.prefix}/lib64
config.javadir=${config.datadir}/java
config.javadocdir=${config.datadir}/javadoc
config.psylladir=${config.datadir}/${project.name}
config.licensedir=${config.datadir}/licenses
config.site.bindir=${config.prefix}/bin
config.site.libdir=${config.datadir}/${project.name}/site
config.vendor.bindir=${config.prefix}/bin
config.vendor.libdir=${config.datadir}/${project.name}/${project.version}
config.mandir=${config.datadir}/man
config.docdir=${config.datadir}/doc/packages
config.javadocdir=${config.datadir}/javadoc
config.class.path=
config.library.path=.:${config.vendor.libdir}:${config.site.libdir}
config.vendor.name=Anton Shvetz
config.vendor.url=https://github.com/urbic/${project.name}
config.vendor.site=https://urbic.github.io/${project.name}
