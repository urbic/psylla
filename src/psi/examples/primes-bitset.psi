#!/usr/bin/psyche

/primes bitset def

/testprime
{
	true exch
	//primes
	{
		1 index exch
		mod iszero
		{
			exch not exch
			exit
		}
		if
	}
	forall
	1 index
	{ //primes exch append } { pop } ifelse
}
bind def

2 1 arguments 0 get tointeger
{
	dup
	testprime
	{ tostring say }
	{ pop }
	ifelse
}
bind for
