/testprime
{
	true exch
	2 1 2 index
	1 sub
	{
		2 copy
		dup mul
		lt { pop exit } if

		1 index exch
		mod
		0 eq
		{
			exch not exch
			exit
		}
		if
	}
	for
	pop
}
bind def

2 1 arguments 1 get tointeger
{
	dup
	testprime
	{ tostring say }
	{ pop }
	ifelse
}
bind for

quit
