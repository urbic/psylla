#!/usr/bin/psylla

arguments isempty { arguments '-' append } if

arguments
{
	dup '-' ne { filereader } { stdin } ifelse
	/in exch def

	{
		in 4096 readstring { print } { exit } ifelse
	}
	loop

	stdout flush
	in close
}
bind forall
