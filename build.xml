<?xml version="1.0"?>
<!--
$URL$
$Id$
-->
<project name="psi" default="dist" basedir=".">
	
	<property name="project.name" value="${ant.project.name}"/>
	<property name="sources" location="sources"/>
	<property name="sources.java" location="${sources}/java"/>
	<property name="sources.jj" location="${sources}/jj"/>
	<property name="sources.docbook" location="${sources}/docbook"/>
	<property name="build" location="build"/>
	<property name="build.sources.java" location="${build}/sources/java"/>
	<property name="build.doc" location="${build}/doc"/>
	<property name="classes" location="${build}/classes"/>
	<property name="dist"  location="dist"/>
	<property name="dist.lib"  location="${dist}/lib"/>
	<property name="javadoc"  location="javadoc"/>
	<property name="test.file"  location="test.psi"/>
	<!-- <property name="java.library.path"  location="."/> -->
	<!-- <property name="java.library.path" location="/home/tz/work/projects/jslangy"/> -->

	<presetdef name="xslt.preset">
		<xslt
			style="${sources.docbook}/xhtml.xsl"
			basedir="${sources.docbook}"
			filedirparameter="${sources.docbook}"
			force="true"
			>
			<factory name="com.icl.saxon.TransformerFactoryImpl"/>
			<xmlcatalog>
				<catalogpath>
					<pathelement location="${sources.docbook}/xmlcatalog"/>
				</catalogpath>
			</xmlcatalog>
			<classpath>
				<pathelement location="/usr/share/java/saxon6.jar"/>
				<pathelement location="/usr/share/java/xml-commons-resolver.jar"/>
				<pathelement location="/usr/share/xml/docbook/stylesheet/nwalsh5/current/extensions/saxon65.jar"/>
				<pathelement location="/usr/share/java/xslthl.jar"/>
			</classpath>
			<param name="highlight.source" expression="1"/>
			<param name="profile.status" expression="${xslt.profile.status}"/>
			<param name="rootid" expression="${xslt.rootid}" if="${xslt.rootid}"/>
			<param name="shortcut.icon" expression="${xslt.shortcut.icon}"/>
			<!--param name="base.dir" expression="${site.build.dir}"/-->
			<param name="html.stylesheet" expression="${project.name}.css"/>
			<param name="link.mailto.url" expression="${xslt.link.mailto.url}"/>
			<param name="generate.manifest" expression="0"/>
			<param name="bibliography.numbered" expression="1"/>
			<!--param name="icon.path" expression="img/icons/16x16/${project.name}.png"/-->
			<param name="tablecolumns.extension" expression="${xslt.tablecolumns.extension}"/>
			<param name="table.borders.with.css" expression="${xslt.table.borders.with.css}"/>
			<param name="l10n.gentext.default.language" expression="'ru'"/>
			<param name="bibliography.style" expression="'iso960'"/>
			<param name="draft.mode" expression="'maybe'"/>
			<param name="use.webcounters" expression="${xslt.use.webcounters}"/>
			<param name="highlight.xslthl.config" expression="file://${xslt.highlight.xslthl.config}"/>
			<param name="navig.graphics.path" expression="navigation/"/>
			<param name="navig.graphics.extension" expression=".svg"/>
		</xslt>
	</presetdef>

	<macrodef name="xslt.lang">
		<attribute name="base.name"/>
		<attribute name="lang"/>
		<sequential>
			<xslt.preset
				includes="@{base.name}_@{lang}.docbook"
				destdir="${build.doc}"
				>
				<param name="root.filename" expression="@{base.name}_@{lang}"/>
				<param name="html.ext" expression=".xhtml"/>
				<param name="base.dir" expression="${build.doc}"/>
			</xslt.preset>
		</sequential>
	</macrodef>	


	<target name="compile.jj">
		<mkdir dir="${build.sources.java}/coneforest/psi"/>
		<javacc
			target="${sources.jj}/coneforest/psi/Parser.jj"
			outputdirectory="${build.sources.java}/coneforest/psi"
			javacchome="/usr/share/java"
			unicodeinput="true"
			/>
	</target>

	<target name="compile.java" depends="compile.jj"
			description="compile the source ">
		<mkdir dir="${classes}"/>
		<javac
			sourcepath="${sources.java}:${build.sources.java}"
			srcdir="${sources.java}"
			destdir="${classes}"
			includeantruntime="false"
			>
		</javac>
	</target>

	<target name="compile" depends="compile.java"/>
	
	<target name="javadoc">
		<mkdir dir="${javadoc}"/>
		<javadoc
			sourcepath="${sources.java}"
			destdir="${javadoc}"
			access="public"
			/>
	</target>

	<target name="jar" depends="compile">
		<jar jarfile="${dist.lib}/${ant.project.name}.jar" basedir="${classes}">
			<manifest>
				<attribute name="Main-Class" value="coneforest.psi.Psi"/>
				<!--attribute name="Class-Path" value="/usr/share/java/regexp.jar"/-->
			</manifest>
		</jar>
	</target>

	<target name="clean" description="Clean build directory">
		<delete dir="${build}"/>
	</target>

	<target name="test" depends="jar">
		<java
			jar="${dist.lib}/${ant.project.name}.jar"
			fork="true"
			>
			<arg file="${test.file}"/>
		</java>
	</target>

	<target name="doc" description="Make documentation" depends="css">
		<mkdir dir="${build.doc}"/>
		<symlink resource="${sources.docbook}/examples" link="examples" overwrite="true"/>
		<!--xslt.lang lang="en"/-->
		<xslt.lang lang="ru" base.name="PSIReference"/>
		<copy todir="${build.doc}">
			<fileset dir="${sources.docbook}" includes="*.png *.svg *.jpeg *.gif *.js"/>
			<fileset dir="${sources.docbook}" includes="examples/**" excludes="**/.htaccess"/>
			<fileset dir="${sources.docbook}" includes="files/**" excludes="**/.htaccess"/>
			<fileset dir="${sources.docbook}" includes="navigation/**"/>
			<fileset dir="${sources.docbook}" includes="js/**"/>
		</copy>
		<!--copy todir="${build.doc}">
			<fileset dir="${sources.dir}" includes="MathJax4SVG.js"/>
		</copy-->
		<!--symlink resource="../MathJax4SVG.js" link="${site.build.dir}/examples/MathJax4SVG.js" overwrite="true"/-->
	</target>
	
	<target name="css" description="Make CSS">
		<mkdir dir="${build.doc}"/>
		<dependset>
			<srcfilelist dir="${sources.docbook}" files="*.scss"/>
			<targetfileset dir="${build.doc}" includes="${project.name}.css"/>
		</dependset>
		<apply executable="sass" dest="${build.doc}" parallel="false">
			<arg value="--style"/>
			<arg value="compact"/>
			<!--arg value="-+-load-path"/>
			<arg value="${share.dir}/css"-->
			<srcfile/>
			<targetfile/>
			<fileset dir="${sources.docbook}" includes="${project.name}.scss"/>
			<mapper type="glob" from="*.scss" to="*.css"/>
		</apply>
	</target>


</project>
